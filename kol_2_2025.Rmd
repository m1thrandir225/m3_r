---
title: "Kolok_2_2025"
output: html_notebook
---

# Exercise 1

Со цел да се процени конзистентноста на квалитетот на произведените сијалици во една фабрика,
односно да се види дали нивниот животен рок значително варира, од одделот за контрола на
квалитетот случајно избрале 50 произведени сијалици и го забележале нивниот животен рок (во
часови): 1007, 1023, 972, 929, 997, 1012, 946, 999, 1001, 987, 986, 958, 1016, 912, 1025, 952, 970,
946, 899, 908, 894, 943, 964, 948, 996, 1042, 937, 945, 906, 922, 959, 908, 894, 894, 928, 977, 1007,
937, 983, 923, 857, 935, 930, 974, 908, 911, 927, 962, 895, 924. Од дадените податоци, пресметајте
99% интервал на доверба за дисперзијата на животниот век на една сијалица. Се претпоставува дека
животниот век на една сијалица има нормална распределба.

```{r}
x <- c(1007, 1023, 972, 929, 997, 1012, 946, 999, 1001, 987, 986, 958, 1016, 912, 1025, 952, 970,
946, 899, 908, 894, 943, 964, 948, 996, 1042, 937, 945, 906, 922, 959, 908, 894, 894, 928, 977, 1007,
937, 983, 923, 857, 935, 930, 974, 908, 911, 927, 962, 895, 924)
average <- sum(x)/50
average
sd (x)
var.conf.int <- function(x, alpha = 0.05) {
  lower <- (length(x) - 1) * var(x) / qchisq(alpha / 2, df = (length(x) - 1), lower.tail=FALSE)
  upper <- (length(x) - 1) * var(x) / qchisq(1 - (alpha /2), df = (length(x) - 1), lower.tail = FALSE)
  return(c(lower, upper))
}
var.conf.int(x, alpha = 0.01)

```

# Exercise 2

Една фармацевтска компанија развила ново средство за спиење кое, како што тврдат, помага на
пациентите побрзо да заспијат. Во рамки на еден клинички центар докторите одлучиле да го тестираат
ова тврдење со случаен избор на 10 пациенти кои го користеле новото средство за спиење,
запишувајќи ги нивните времиња на заспивање (во минути): 15, 20, 16, 20, 21, 22, 18, 17, 21 21.
Вообичаено, без какво било средство за спиење, очекуваното време на заспивање на пациентите е 25
минути. Под претпоставка дека времето на заспивање има нормална распределба, со ниво на
значајност од 5% проверете дали новото средство за спиење го намалило очекуваното време на
заспивање.

## Solution

$$
H_0: \mu = 25,
$$

$$
H_a: \mu < 25
$$

```{r}
x <- c(15, 20, 16, 20, 21, 22, 18, 17, 21, 21)
n <- 10
result <- t.test(x, mu = 25, alternative = "less")
result
alpha <- 0.05
alpha < result$p.value
# We reject H_0 
```

# Exercise 3

Еден производител на компјутерски SSD дискови од ист тип ги пакува во пакување од по 2 диска. За
тестирање на квалитетот на новата производна линија, производителот на случаен начин избира 200
пакувања и тестира во секое пакување, колку од дисковите се исправни. Податоците што ги добил се
следните:

## Solution

```{r}
probs <- dbinom(c(0, 1, 2), size = 2, prob = 0.8)
freqs <- c(8, 20, 172)
result <- chisq.test(freqs, p = probs)
alpha <- 0.05
alpha < result$p.value
# alpha < p value -> H_0 is rejected, i.e X doesn't have B(2, 0.8)
p1 <- dbinom(0, size = 2, prob = 0.8)
p1
p2 <- dbinom(1, size = 2, p = 0.8)
p3 <- dbinom(2, size = 2, p = 0.8)
pk <- c (p1, p2, p3)
pk
np1 <- 200 * p1 
np2 <- 200 * p2 
np3 <- 200 * p3
npk <- c (np1, np2, np3)
npk
```

# Exercise 4 

Еден производител на компјутерски SSD дискови од ист тип ги пакува во пакување од по 2 диска. За
тестирање на квалитетот на новата производна линија, производителот на случаен начин избира 200
пакувања и тестира во секое пакување, колку од дисковите се исправни. Податоците што ги добил се
следните:

а) Во R, симулирајте 100 вредности за случајната променлива 𝑋.

б) Пресметајте ја релативната фреквенција дека во банката пристигнале не повеќе од 20 клиенти во еден час од симулираниот примерок и споредете ја со теоретската веројатност

в) Пресметајте просек, стандардна девијација, медијана и ранг за симулираниот примерок.

## Solution

```{r}
x <- rpois(100, lambda = 30)
x
empirical.prob <- length(x[x <= 20]) / length(x)
empirical.prob
theoretical.prob <- ppois(20, lambda = 30)
theoretical.prob
mean(x)
range(x)
sd(x)

```

# Exercise 5

Со цел да се процени просечното време на достава, во рамки на една логистичка компанија случајно
избрале 50 пратки и го забележале нивното време на достава (во часови): 3, 5, 3, 5, 5, 3, 3, 3, 3, 3, 2, 7,
3, 5, 6, 4, 4, 5, 4, 6, 4, 5, 2, 5, 4, 5, 3, 5, 4, 1, 6, 5, 4, 6, 4, 3, 6, 6, 3, 4, 4, 4, 5, 5, 1, 3, 5, 5, 7, 3. Од претходно е познато дека времето на достава има нормална распределба со стандардна девијација 2 часа. Од дадените податоци, пресметајте 99% интервал на доверба за очекуваното време на достава на една пратка.

## Solution 

```{r}
x <- c(3, 5, 3, 5, 5, 3, 3, 3, 3, 3, 2, 7, 3, 5, 6, 4, 4, 5, 4, 6, 4, 5, 2, 5, 4, 5, 3, 5, 4, 1, 6, 5, 4, 6, 4, 3, 6, 6, 3, 4, 4, 4, 5, 5, 1, 3, 5, 5, 7, 3)
n <- 50
average <- sum(x)/n
average
sd(x)
library("BSDA")
z.test(x, sigma.x = 2, conf.level = 0.99)$conf.int
```

# Exercise 6 

Еден производител на батерии тврди дека стандардната девијација на животниот век на батериите е
4 месеци. Од одделот за проверка на квалитет се сомневаат дека стандардната девијација на
животниот век е поголема од тоа колку што тврди производителот, што повлекува дека постои
загриженост за квалитетот на батериите. За да ја проверат својата претпоставка, тие случајно избрале
12 батерии, забележувајќи го нивниот животен век: 24, 22, 28, 21, 32, 25, 30, 20, 26, 29, 33, 19. Под
претпоставка дека животниот век на една батерија има нормална распределба, со ниво на значајност
5% проверете дали стандардната девијација на животниот век на една батерија е поголема од 4
месеци.

## Solution 

$$
H_0: \sigma = 4,
$$

$$
H_a: \sigma > 4 
$$

```{r}
x <- c (24, 22, 28, 21, 32, 25, 30, 20, 26, 29, 33, 19)
x
chsqi.stats <- (length(x) - 1) * var(x) / 4^2
chsqi.stats
lower.bound <- qchisq(0.05, df = 11, lower.tail = FALSE)
lower.bound
chsqi.stats > lower.bound
# H_0 is not rejected, i.e H_0 is true
```

# Exercise 7

Се разгледува бројот на коли коишто пристигнуваат на еден паркинг во текот на 5 минути. На случаен
начин е земен примерок од 100, временски дисјунктни пет минутни интервали и бројот на возила
коишто пристигнале на паркингот е даден во следната табела:

а) Со ниво на значајност од 10% да се испита дали бројот на возила коишто пристигнуваат на паркингот
во текот на 5 минути има 𝑃(2) распределба.

## Solution

$$
H_0: X~P(2)
$$

$$
H_a: X ~! P(2)
$$

```{r}
p0 <- dpois(0, lambda = 2)
p1 <- dpois(1, lambda = 2)
p2 <- ppois(2, lambda = 2, lower.tail = FALSE) + dpois(2, lambda = 2)
probs <- c(p0, p1, p2)
probs
sum(probs)
freqs <- c(10, 65, 25)
result <- chisq.test(freqs, p = probs)
alpha <- 0.01
alpha < result$p.value
```

```{r}
p0 <- dpois(0, lambda = 2)
p1 <- dpois(1, lambda = 2)
p2 <- ppois(2, lambda = 2, lower.tail = FALSE) + dpois(2, lambda = 2)
pk <- c(p0, p1, p2)
pk
sum(pk)
np0 <- 100 * p0
np1 <- 100 * p1
np2 <- 100 * p2
npk <- c(np0, np1, np2)
npk

Nk_np0 <- 10 - np0
Nk_np1 <- 65 - np1
Nk_np2 <- 25 - np2
Nk_npk <- c(Nk_np0, Nk_np1, Nk_np2)
Nk_npk

Nk_npkSQ <- c(Nk_np0^2, Nk_np1^2, Nk_np2^2)
Nk_npkSQ

CHI_SQ <- c(Nk_np0^2 / np0, Nk_np1^2 / np1, Nk_np2^2 / np2)
sum(CHI_SQ)

```

# Exercise 8

Една маркетинг агенција испраќа реклама до 1 000 корисници преку е-пошта. Врз основа на претходни истражувања заклучиле дека еден корисник ги чита рекламите испратени преку е-пошта во 20% од случаите. Нека 𝑋 е случајна променлива дефинирана како „број на корисници што ја прочитале рекламата испратена преку е-пошта од страна на маркетинг агенцијата“.

а) Во R, симулирајте 100 вредности за случајната променлива 𝑋.

б) Пресметајте ја релативната фреквенција дека рекламата ја прочитале не повеќе од 210 корисници од симулираниот примерок и споредете ја со теоретската веројатност.

в) Пресметајте просек, стандардна девијација, медијана и ранг за симулираниот примерок.

## Solution

```{r}
x <- rbinom(100, size = 100, prob = 0.2)
x
empirical.prob <- length(x[x<= 210]) / length(x)
empirical.prob
theoretical.prob <- pbinom(210, size = 1000, prob = 0.2)
theoretical.prob
mean(x)
sd(x)
median(x)
range(x)
```
