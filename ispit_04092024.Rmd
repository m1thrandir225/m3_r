---
title: "Ispit 04.09.2024"
output: html_notebook
---

# Exercise 13 

Нека бројот на поени што ги постигнува кошаркарскиот тим на Работнички има нормална
распределба со непознато математичко очекување 𝜃. За да се оцени непознатиот параметар 𝜃, земен е примерок од 10 натпревари и добиено е дека просечниот број на постигнати поени е 65 со стандардна девијација 25.

а) (5) Да се определи 95% интервал на доверба за непознатиот параметар 𝜃.

```{r}
n <- 10
average <- 65
s <- 25
alpha <- 0.05
t_stat <- qt(alpha / 2, df = 10 - 1, lower.tail = FALSE)
t_stat
lower <- average - ((t_stat * s) / sqrt(n))
upper <- average + ((t_stat *s) / sqrt(n))
bounds <- c(lower, upper)
bounds
```

в) (5) Со ниво на значајност 0.05, да се тестира дали очекуваниот број на постигнати поени по
натпревар е 69 или е помал од 69.

$$
H_0: \mu = 69,
$$

$$
H_a: \mu \neq 69
$$

```{r}
test <- ((average - 69) / 25) * sqrt(10)
test
bound <- qt(0.05, 9)
test < bound
# since the p value is lower than the upper bound of the critical domain, the H_0 hypothesis is rejected, i.e the expected number of points is lower than 69
```

# Exercise 14 

Една фабрика за пакување на апетисани прави контрола на своето производство. Според
плановите, фабриката треба да пакува 50% кикирики, 20% лешници, 15% бадеми и 15% ф’стаци. За да се провери дали очекуваниот однос на апетисаните е реализиран, земени се 1000 пакетчиња апетисани и утврдено е дека 621 пакетче се со кикирики, 175 пакетчиња лешници, 114 пакетчиња бадеми и 90 пакетчиња ф’стаци. Со ниво на значајност 0.05, да се тестира дали затекнатата распределба одговара на планираната.

```{r}
probs <- c(0.5, 0.2, 0.15, 0.15)
freqs <- c(621, 175, 114, 90)
res <- chisq.test(freqs, p = probs)
alpha <- 0.05 
alpha < res$p.value
# p_value is lower than the alpha -> we reject H_0 
```

# Exercise 15

Нека бројот на поени што ги постигнува кошаркарскиот тим на МЗТ има нормална распределба со непознато математичко очекување 𝜃. За да се оцени непознатиот параметар 𝜃, земен е примерок од 12 натпревари и добиено е дека просечниот број на постигнати поени е 72 со стандардна девијација 25.
а) (5) Да се определи 99% интервал на доверба за непознатиот параметар 𝜃.

```{r}
n <- 12
average <- 72
s <- 25
alpha <- 0.01
t_test <- qt(alpha / 2, df = n - 1, lower.tail = FALSE)
t_test
lower <- average - (t_test * s / sqrt(n))
upper <- average + (t_test * s / sqrt(n))
bounds <- c(lower, upper)
bounds
```

в) (5) Со ниво на значајност 0.01, да се тестира дали очекуваниот број на постигнати поени по
натпревар е 70 или помал од 70.

$$
H_0: \mu = 70,
$$

$$
H_a: \mu < 70
$$

```{r}
alpha <- 0.01
mu_0 <- 70
# normal distribution small sample, unknown variance -> t test
t_0 <- ((average - mu_0) / s) * sqrt(n)
bound <- qt(alpha, df = n - 1, lower.tail = FALSE)
t_0
t_0 > -Inf && t_0 < -bound # is it in critical domain?
# since t_0 is not in the critical domain, we reject the H_0 hypothesis, i.e the expected value of points is lower than 70
```

# Exercise 16

Познато е дека во еден поголем маркет, во летните месеци, во продажбата на безалкохолни
пијалоци 60% се вода, 20% Кока Кола, 15% Фанта и 5% останати пијалоци. Во месец август
продажбата продадени се 653 шишиња вода, 164 шишиња Кока Кола, 102 шишиња Фанта и 81
шише останатите безалкоголни пијалоци. Со ниво на значајност 0.05, да се тестира дали
продажбата во месец август соодветствува на вообичаената продажба во летните месеци.

```{r}
probs <- c(0.6, 0.2, 0.15, 0.05)
freq <- c(653, 164, 102, 81)
alpha <- 0.05
res <- chisq.test(freq, p = probs)
alpha < res$p.value
# since p_value is not bigger than alpha -> H_0 is rejected
```
